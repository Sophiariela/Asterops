<div class="container section">
  <div class="rich-text rich-text--left" style="max-width: 760px;">
    <h1>{{ page.title }}</h1>
    <div class="rich-text__body">{{ page.content }}</div>
  </div>
</div>

{% schema %}
{
  "name": "t:sections.main-page.name"
}
{% endschema %}
